<Project Sdk="Microsoft.NET.Sdk">
  <!-- Pure library: the helper's state, its command lines, name matching, the IPC payloads. BCL only, no Dalamud. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>XivArcade.Core</RootNamespace>
    <AssemblyName>XivArcade.Core</AssemblyName>
    <Version>0.1.0.0</Version>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="XivArcade.Core.Tests" />
  </ItemGroup>
</Project>
